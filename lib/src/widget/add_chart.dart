import 'package:flutter/material.dart';

class AddChartButton extends StatelessWidget {
  final double quantity;
  AddChartButton({required this.quantity});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
